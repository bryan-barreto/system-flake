# Guide
This is a very basic starter system level flake.
Be sure to set your username and hostname.
Always use an admin account to change your username with passwd if redefining the user.

## Usage
```bash
sudo ./run.sh
```
